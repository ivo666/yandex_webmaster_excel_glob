# Yandex Webmaster Excel Glob (ETL Pipeline)

Автоматизированный многослойный конвейер (ETL/ELT) для сбора, валидации, санитарной очистки и хранения данных поисковой аналитики Яндекс Вебмастера в СУБД PostgreSQL.

## 🏗 Архитектура данных

Проект построен по методологии разделения слоев хранения хранилища данных (DWH):
1. **RDL (Raw Data Layer)** — слой сырых данных. Содержит слепки выгрузок в неизменном виде. Реализована *идемпотентность* — при повторной загрузке пересекающиеся даты автоматически перезаписываются, защищая базу от дубликатов.
2. **PPL (Pre-processed Layer)** — слой предобработки. Выполняет функции **Data Quality**:
   * Очищает URL (`page_path`) от мусорных динамических GET-параметров.
   * Восстанавливает бизнес-логику воронки: подтягивает спрос до показов (`demand >= impressions`) и срезает аномальные клики до уровня показов (`clicks <= impressions`).

🏗 Архитектура данных
Проект построен по методологии разделения слоев хранения хранилища данных (DWH):

1. RDL (Raw Data Layer) — слой сырых данных. Содержит слепки выгрузок в неизменном виде. Реализована идемпотентность.
2. PPL (Pre-processed Layer) — слой предобработки. Выполняет Data Quality (очистку URL, восстановление бизнес-логики воронки).
   - ppl_lookup — буферные накопительные реестры уникальных сущностей (Staging зона).
3. 3. **DDM (Data Delivery Marts)** — аналитический слой (схема «Звезда»). Данные атомизированы и защищены от дублирования инкрементальными загрузчиками:
   * `date_dimension` — календарный справочник с автоматически вычисляемыми бизнес-атрибутами (день недели, выходные).
   * `query_dimension` / `page_dimension` — чистые справочники измерений с суррогатными ID. Реализована дедупликация на уровне Polars (Anti-Join), защищающая автоинкрементные счетчики PostgreSQL от холостых срабатываний.
   * `fct_row_demand` — атомарный спрос, связанный напрямую с ID исходных строк слоя PPL, что исключает кратное завышение спроса при аналитических джойнах.
   * `fct_page_impressions_clicks` — атомарные факты показов и кликов, развернутые построчно с фиксацией позиций *(в разработке)*.



## 🛠 Технологический стек
* **Python 3.11+**
* **Polars** — сверхбыстрая обработка и трансформация данных в оперативной памяти.
* **SQLAlchemy 2.0 (Core / ORM)** — транзакционное управление базами данных и пакетная вставка (Bulk Insert).
* **SSHTunnel & Paramiko** — безопасное туннелирование закрытого порта удаленного сервера базы данных через SSH.

## 📁 Структура проекта
```text
04 SQLA/
├── data/
│   ├── input/          # Папка для входящих файлов (Excel / исторический CSV)
│   └── archive/        # Архив успешно обработанных файлов с временными метками
├── src/
│   ├── database/
│   │   ├── connection.py  # Управление SSH-туннелем и сессиями SQLAlchemy
│   │   └── repository.py  # Логика работы с запросами к таблицам
│   ├── ddm/
│   │   ├── __init__.py
│   │   ├── dates_loader.py # Инкрементальный загрузчик измерений дат
│   │   ├── fct_demand_loader.py # Инкрементальный загрузчик фактов спроса
│   │   ├── page_paths_loader.py # Инкрементальный загрузчик измерений страниц
│   │   └── queries_loader.py # Инкрементальный загрузчик измерений запросов
│   ├── models/
│   │   └── models.py      # Декларативные ORM-модели таблиц всех схем (rdl/ppl/ddm)
│   ├── ppl/
│   │   └── ppl_loader.py  # Класс предобработки и расчета витрин
│   ├── utils/
│   │   └── load_history.py # Скрипт миграции исторических данных из CSV
│   └── load_data.py       # Парсинг Excel и загрузка слоя RDL
├── .env.example        # Шаблон конфигурации переменных окружения
├── .gitignore          # Исключения для Git (venv, .env, кэш)
├── main.py             # Главный оркестратор (точка входа конвейера)
└── requirements.txt    # Зависимости проекта
```


## 🚀 Быстрый запуск

1. **Клонируйте репозиторий и настройте окружение:**
   ```bash
   git clone <ссылка_на_ваш_репозиторий>
   cd yandex_webmaster_excel_glob
   python -m venv venv
   source venv/bin/activate  # Для Windows: venv\Scripts\activate
   pip install -r requirements.txt
   ```

2. **Настройте переменные окружения:**
   Создайте файл `.env` в корне проекта на основе `.env.example` и укажите доступы к вашему SSH и PostgreSQL.

3. **Запустите конвейер:**
   Положите Excel-выгрузку Вебмастера в `data/input/` и выполните:
   ```bash
   python main.py
   ```
